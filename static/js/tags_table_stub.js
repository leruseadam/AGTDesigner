// TagsTable stub to prevent undefined errors
window.TagsTable = {
    updateTagsList: function(containerId, tags) {
        console.log('TagsTable.updateTagsList called:', containerId, tags?.length || 0, 'tags');
        // Stub implementation - actual functionality handled by TagManager
    },
    
    updateTableHeader: function() {
        console.log('TagsTable.updateTableHeader called');
        // Stub implementation - actual functionality handled by TagManager
    }
};

console.log('TagsTable stub loaded');
