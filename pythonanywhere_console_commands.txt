# PythonAnywhere Console Commands
# Copy and paste these commands into your PythonAnywhere Bash console

# Navigate to your project directory
cd ~/AGTDesigner

# Check if uploads directory exists
ls -la uploads/

# Create uploads directory if it doesn't exist
mkdir -p uploads

# Create a simple test file with real data
python3 -c "
import pandas as pd
import os

# Create sample data
data = {
    'Product Name*': ['Purple Sour Diesel - 3.5g', 'Blue Dream - 3.5g', 'OG Kush - 3.5g'],
    'Vendor': ['A Greener Today', 'A Greener Today', 'A Greener Today'],
    'Brand': ['Test Brand', 'Test Brand', 'Test Brand'],
    'Product Type': ['Flower', 'Flower', 'Flower'],
    'Weight': ['3.5g', '3.5g', '3.5g'],
    'Strain': ['Purple Sour Diesel', 'Blue Dream', 'OG Kush'],
    'Lineage': ['Hybrid', 'Hybrid', 'Indica'],
    'THC %': ['22.5', '18.2', '24.1'],
    'CBD %': ['0.1', '0.2', '0.1']
}

# Create DataFrame
df = pd.DataFrame(data)

# Save to Excel file
output_path = 'uploads/testFile.xlsx'
df.to_excel(output_path, index=False)

print(f'✅ Created test file: {output_path}')
print(f'📊 File size: {os.path.getsize(output_path)} bytes')
print(f'📋 Records: {len(df)}')
"

# Verify the file was created
ls -la uploads/

# Restart the web app
echo "Now go to the Web tab and click Reload on your web app" 