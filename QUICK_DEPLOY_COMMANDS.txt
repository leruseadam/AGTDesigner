🚀 QUICK DEPLOYMENT COMMANDS FOR PYTHONANYWHERE
================================================

Copy and paste these commands one by one:

# Step 1: Navigate to the correct directory
cd ~/AGTDesigner

# Step 2: Pull the latest code
git pull origin main

# Step 3: Verify the fix (optional)
python3 verify_lineage_fix.py

# Step 4: Test lineage functionality (optional)
python3 test_lineage_end_to_end.py

# Step 5: Check database
python3 -c "import sqlite3; conn = sqlite3.connect('uploads/product_database.db'); print(f'Products: {conn.execute(\"SELECT COUNT(*) FROM products\").fetchone()[0]}'); conn.close()"

================================================

After running these commands:

1. Go to: https://www.pythonanywhere.com/user/adamcordova/webapps/
2. Click the green "Reload www.agtpricetags.com" button
3. Wait 30 seconds
4. Clear your browser cache: Ctrl+Shift+R (Windows) or Cmd+Shift+R (Mac)
5. Test at: https://www.agtpricetags.com

================================================

Monitor logs (optional):
tail -f /var/log/www.agtpricetags.com.error.log

Press Ctrl+C to stop monitoring logs

================================================

