
╔══════════════════════════════════════════════════════════════════════╗
║                PythonAnywhere Upload Instructions                    ║
╚══════════════════════════════════════════════════════════════════════╝

📦 Archive Ready: database_sync_to_pythonanywhere_20251012_130445.zip
📍 Location: /Users/adamcordova/Desktop/labelMaker_ QR copy final/database_sync_to_pythonanywhere_20251012_130445.zip

🔧 OPTION 1: Upload via Web Console (Recommended)
───────────────────────────────────────────────────────
1. Go to: https://www.pythonanywhere.com/user/YOUR_USERNAME/files/
2. Navigate to your app directory (usually /home/YOUR_USERNAME/)
3. Click "Upload a file" button
4. Select: database_sync_to_pythonanywhere_20251012_130445.zip
5. Wait for upload to complete

Then in the PythonAnywhere Bash console, run:
   cd /home/YOUR_USERNAME/
   unzip database_sync_to_pythonanywhere_20251012_130445.zip
   mv product_database.db uploads/
   # If backup included:
   mkdir -p uploads/backups
   mv backup_*.db uploads/backups/
   
   # Verify the database
   python3 << 'EOF'
import sqlite3
conn = sqlite3.connect('uploads/product_database.db')
cursor = conn.cursor()
cursor.execute("PRAGMA integrity_check")
result = cursor.fetchone()
print(f"Database integrity: {result[0]}")
cursor.execute("SELECT COUNT(*) FROM products")
print(f"Products: {cursor.fetchone()[0]}")
cursor.execute("SELECT COUNT(*) FROM strains")
print(f"Strains: {cursor.fetchone()[0]}")
conn.close()
EOF

   # Reload your web app
   # Go to Web tab and click the green "Reload" button

🔧 OPTION 2: Upload via SCP (Advanced)
───────────────────────────────────────────────────────
   scp /Users/adamcordova/Desktop/labelMaker_ QR copy final/database_sync_to_pythonanywhere_20251012_130445.zip YOUR_USERNAME@ssh.pythonanywhere.com:~/
   
   Then SSH in and run the unzip commands above.

🔧 OPTION 3: Use the provided script
───────────────────────────────────────────────────────
   ./upload_to_pythonanywhere.sh

⚠️  IMPORTANT NOTES:
───────────────────────────────────────────────────────
• Make sure to backup your existing PythonAnywhere database first!
• The new database will replace the existing one
• After upload, test your app thoroughly
• The health monitoring system will continue to protect your database

✅ DATABASE VERIFICATION
───────────────────────────────────────────────────────
After uploading, verify the database health by accessing:
   https://YOUR_APP.pythonanywhere.com/api/database-health

You should see a JSON response with is_healthy: true

╔══════════════════════════════════════════════════════════════════════╗
║  Database is ready for deployment! Good luck! 🚀                     ║
╚══════════════════════════════════════════════════════════════════════╝
