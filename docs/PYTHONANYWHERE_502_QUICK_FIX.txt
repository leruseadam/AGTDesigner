======================================================================
QUICK FIX FOR 502 BAD GATEWAY ERROR ON PYTHONANYWHERE
======================================================================

ERROR YOU'RE SEEING:
- GET https://www.agtpricetags.com/api/initial-data 502 (Bad Gateway)
- GET https://www.agtpricetags.com/api/available-tags 502 (Bad Gateway)

WHAT IT MEANS:
Your Flask app is not running or crashing on PythonAnywhere.

======================================================================
TRY THESE FIXES IN ORDER (STOP WHEN ONE WORKS)
======================================================================

FIX #1: RELOAD THE WEB APP (90% success rate)
----------------------------------------------------------------------
1. Go to: https://www.pythonanywhere.com/user/adamcordova/webapps
2. Click the green "Reload" button (top right)
3. Wait 15 seconds
4. Test site: https://www.agtpricetags.com
5. Clear browser cache: Ctrl+Shift+R (or Cmd+Shift+R on Mac)

If this doesn't work, continue to Fix #2.

FIX #2: CHECK ERROR LOGS
----------------------------------------------------------------------
1. On PythonAnywhere Web tab, scroll down to "Log files"
2. Click "Error log" (the latest one)
3. Scroll to the bottom
4. Look for Python errors

COMMON ERRORS YOU MIGHT SEE:
- ModuleNotFoundError → Go to Fix #3
- FileNotFoundError → Go to Fix #4  
- ImportError → Go to Fix #3
- No error at all → Go to Fix #5

FIX #3: INSTALL MISSING PACKAGES
----------------------------------------------------------------------
Open a Bash console on PythonAnywhere:

cd /home/adamcordova/AGTDesigner
pip3 install --user flask flask-cors flask-session pandas openpyxl python-docx qrcode pillow flask-compress

Then reload web app (green button on Web tab).

FIX #4: CHECK DATABASE EXISTS
----------------------------------------------------------------------
Open a Bash console on PythonAnywhere:

ls -la /home/adamcordova/AGTDesigner/uploads/product_database_AGT_Bothell.db

If you see "No such file or directory":
1. Go to Files tab
2. Navigate to /home/adamcordova/AGTDesigner/uploads/
3. Upload your database file: product_database_AGT_Bothell.db
4. Reload web app

FIX #5: RUN HEALTH CHECK
----------------------------------------------------------------------
1. Upload check_pythonanywhere_health.py to /home/adamcordova/AGTDesigner/
2. Open Bash console:

cd /home/adamcordova/AGTDesigner
python3 check_pythonanywhere_health.py

3. Follow any issues it reports
4. Reload web app after fixing

FIX #6: CHECK PYTHON VERSION
----------------------------------------------------------------------
1. Web tab → Look for "Python version" dropdown
2. Should be Python 3.9 or 3.10 (NOT 2.7!)
3. If wrong, select correct version
4. Reload web app

FIX #7: VERIFY WSGI CONFIGURATION
----------------------------------------------------------------------
1. Web tab → Click "WSGI configuration file" link
2. Make sure line 12 says:
   project_dir = '/home/adamcordova/AGTDesigner'
3. Make sure line 44 says:
   from app import app as application
4. Save if you made changes
5. Reload web app

FIX #8: NUCLEAR OPTION - FORCE RESTART
----------------------------------------------------------------------
If NOTHING works:

1. Web tab → Click "Reload" button
2. Wait 30 seconds
3. Go to Consoles tab → Kill all running consoles
4. Go to Files tab → Check files are there
5. Go back to Web tab → Reload again
6. Wait 30 seconds
7. Test site

======================================================================
AFTER EACH FIX:
======================================================================
1. Click the green "Reload" button on Web tab
2. Wait 15 seconds
3. Clear browser cache (Ctrl+Shift+R)
4. Try accessing: https://www.agtpricetags.com
5. Open browser console (F12) to check for errors

======================================================================
STILL NOT WORKING?
======================================================================

RUN THIS IN BASH CONSOLE:
cd /home/adamcordova/AGTDesigner
python3 -c "from app import app; print('SUCCESS')"

If you see an error, that's the problem - share that error message.

GET FULL ERROR LOG:
tail -50 /var/log/www.agtpricetags.com.error.log
tail -50 /var/log/www.agtpricetags.com.server.log

Copy the output and we can diagnose further.

======================================================================
MOST COMMON CAUSES (in order):
======================================================================
1. ✅ Forgot to reload after deployment (95% of cases)
2. ✅ Missing Python packages
3. ✅ Database file not uploaded
4. ✅ Wrong Python version selected
5. ✅ WSGI configuration pointing to wrong directory

======================================================================

Good luck! Most likely you just need to click the Reload button. 😊

